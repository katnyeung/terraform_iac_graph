package com.terraform.neo4j.config;

import org.springframework.context.annotation.Configuration;

/**
 * Neo4j configuration class that sets up the database connection
 * and transaction management for the Terraform parser application.
 * 
 * With Spring Boot 3.x, most Neo4j configuration is handled automatically
 * through application.yml properties. This class can be extended for
 * custom configuration needs.
 */
@Configuration
public class Neo4jConfig {
    // Spring Boot 3.x handles Neo4j configuration automatically
    // Custom configuration can be added here if needed
}